@model IEnumerable<VirtoCommerce.ApiClient.DataContracts.Association>

@{
    var associations = Model.OrderBy(a => a.Priority).ToList();
}

<header>
    <h2>@("Related Products".Localize())</h2>
</header>

<div class="scroll">
    <ul class="products-list slider-list grid">
        @foreach (var association in associations)
        {
            @Html.Action("AssociatedItem", new { itemId = association.ItemId, name = "displaytemplates/Item", associationType = association.Type })
        }
    </ul>
</div>
