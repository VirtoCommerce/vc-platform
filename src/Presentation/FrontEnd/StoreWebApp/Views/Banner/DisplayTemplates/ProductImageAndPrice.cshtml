@using VirtoCommerce.Client
@{
    Layout = null;
	var currentModel = ViewData["Model"] as ProductWithImageAndPriceModel;
}

@if (currentModel != null)
{
    @Html.Action("DisplayItemNoCache", "Catalog", new { item = currentModel.ProductCode, bycode = true, displayOptions = ItemDisplayOptions.ItemMedium, name = "DisplayTemplates/Item" })
}

