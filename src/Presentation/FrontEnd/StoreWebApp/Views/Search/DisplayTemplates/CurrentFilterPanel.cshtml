@model CatalogItemSearchModel

<aside class="block">
    <span class="title">@("Currently shop by".Localize()):</span>
    @if (Model.SelectedFilters != null && Model.SelectedFilters.Count > 0)
    {
        <ol class="currently">
            @foreach (var filter in Model.SelectedFilters)
            {
                <li>
                    <span class="label">@filter.Filter.Name.Localize():</span>
                    <span class="value">@filter.Facet.Name</span>
                    <a class="remove" title="@("Remove This Item".Localize())" href="@Url.RemoveFacet(filter.Filter.Key, filter.Facet.Key)">@("Remove This Item".Localize())</a>
                </li>
            }
        </ol>
    }
</aside>

