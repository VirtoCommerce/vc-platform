# v3.800.11
# bump-ubuntu-version
name: Release

on:
  workflow_dispatch:

jobs:
  release:
    uses: VirtoCommerce/.github/.github/workflows/release.yml@v3.800.11    
    secrets:
      envPAT: ${{ secrets.REPO_TOKEN }}